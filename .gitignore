node_modules/
build/
dist/
functions/lib/
coverage/
.DS_Store
*.log
.env*
!.env.example

# graphify knowledge graph (regenerable; run `graphify update .`)
graphify-out/

# Firebase Admin service-account keys for local dev (NEVER commit)
service-account.json
*serviceaccount*.json
*-firebase-adminsdk-*.json

# Claude Flow worker scratch clones
scripts/sandbox/

# GitHub PAT for scheduled Claude tasks (gh CLI auth in sandbox) — NEVER commit
.github-token
